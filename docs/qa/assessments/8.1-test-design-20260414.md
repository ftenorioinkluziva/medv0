# Test Design: Story 8.1 — Agent-Knowledge Schema & Migration

**Date:** 2026-04-14
**Designer:** Quinn (Test Architect)
**Linear:** BLA-70
**Epic:** E8 — Knowledge Segmentation

## Test Strategy Overview

- Total test scenarios: 19
- Unit tests: 2 (11%)
- Integration tests: 17 (89%)
- E2E tests: 0
- Priority distribution: P0: 10, P1: 9

**Rationale:** Story é 100% backend (schema + migration + query logic). E2E não aplicável. Integration tests dominam porque o comportamento crítico depende de banco real (unique constraints, FKs, query filters).

---

## Test Scenarios by Acceptance Criteria

### AC1 — Tabela `agent_knowledge`

| ID | Level | Priority | Cenário | Justificativa |
|----|-------|----------|---------|---------------|
| 8.1-INT-001 | Integration | P0 | Tabela existe com colunas `id`, `agentId`, `articleId`, `createdAt` e tipos corretos | Schema correctness |
| 8.1-INT-002 | Integration | P0 | INSERT duplicado `(agentId, articleId)` lança constraint violation | UNIQUE constraint crítico |
| 8.1-INT-003 | Integration | P1 | DELETE em `health_agents` remove rows associados em `agent_knowledge` | CASCADE FK — evita orphan records |
| 8.1-INT-004 | Integration | P1 | DELETE em `knowledge_base` remove rows associados em `agent_knowledge` | CASCADE FK — evita orphan records |

### AC2 — Campo `isGlobal` em `knowledge_base`

| ID | Level | Priority | Cenário | Justificativa |
|----|-------|----------|---------|---------------|
| 8.1-INT-005 | Integration | P0 | Campo `is_global` existe com DEFAULT TRUE após migration | Campo ausente quebraria toda lógica de filtro |
| 8.1-INT-006 | Integration | P0 | Todos artigos existentes pré-migration têm `is_global = true` | Backward compatibility |

### AC3 — `searchKnowledge` com filtro por agente

| ID | Level | Priority | Cenário | Justificativa |
|----|-------|----------|---------|---------------|
| 8.1-UNIT-001 | Unit | P0 | `searchKnowledge('query', 5)` sem agentId executa path sem WHERE extra | Lógica de branching — backward compat sem DB |
| 8.1-INT-007 | Integration | P0 | Cenário A: sem agentId retorna embeddings de TODOS os artigos | Backward compat com consumers existentes |
| 8.1-INT-008 | Integration | P0 | Cenário B: com agentId retorna chunks de artigo associado | Core feature — filtro por associação |
| 8.1-INT-009 | Integration | P0 | Cenário C: com agentId retorna chunks de artigo global não associado | Artigos globais são universais |
| 8.1-INT-010 | Integration | P0 | Cenário D: com agentId NÃO retorna chunks de artigo de outro agente (isGlobal=false) | Isolamento de escopo |

### AC4 — Queries helper

| ID | Level | Priority | Cenário | Justificativa |
|----|-------|----------|---------|---------------|
| 8.1-INT-011 | Integration | P1 | `getArticlesByAgent(agentId)` retorna apenas artigos associados | CRUD |
| 8.1-INT-012 | Integration | P1 | `associateArticleToAgent(agentId, articleId)` cria row em `agent_knowledge` | CRUD |
| 8.1-INT-013 | Integration | P1 | `disassociateArticleFromAgent(agentId, articleId)` remove row | CRUD |
| 8.1-INT-014 | Integration | P1 | `getAgentsByArticle(articleId)` retorna agentes associados | CRUD |
| 8.1-INT-015 | Integration | P1 | `associateArticleToAgent` com par existente lança erro | Unique violation controlada |

### AC5 — `analyzeWithAgent` usa filtro

| ID | Level | Priority | Cenário | Justificativa |
|----|-------|----------|---------|---------------|
| 8.1-UNIT-002 | Unit | P0 | `analyzeWithAgent` invoca `searchKnowledge` com `agent.id` como terceiro argumento | Garante que pipeline passa agentId — sem este teste, regressão silenciosa |
| 8.1-INT-016 | Integration | P1 | Agente sem artigos associados recebe chunks de artigos globais | Agentes "vazios" não ficam sem contexto RAG |

### AC6 — Migration preserva dados

| ID | Level | Priority | Cenário | Justificativa |
|----|-------|----------|---------|---------------|
| 8.1-INT-017 | Integration | P0 | Após migration, COUNT artigos = COUNT com `is_global = true` | Integridade de dados — nenhum artigo perdido |

---

## Risk Coverage

| Risco | Cenário(s) | Mitigação |
|-------|------------|-----------|
| MEDIUM: subquery lenta sem índices | 8.1-INT-008, 009, 010 | Validar com dados realistas; confirmar uso de índices `agent_knowledge_agent_id_idx` |
| LOW: backward compat quebrada | 8.1-UNIT-001, 8.1-INT-007 | Path sem agentId testado em unit + integration |

---

## Recommended Execution Order

1. P0 Unit (8.1-UNIT-001, 8.1-UNIT-002) — fail fast, sem DB
2. P0 Integration — schema (8.1-INT-001..006) — valida migration
3. P0 Integration — search (8.1-INT-007..010) — core feature
4. P0 Integration — data integrity (8.1-INT-017)
5. P1 Integration — helpers (8.1-INT-011..016)

---

## Gate YAML Block

```yaml
test_design:
  story: "8.1"
  linear: "BLA-70"
  scenarios_total: 19
  by_level:
    unit: 2
    integration: 17
    e2e: 0
  by_priority:
    p0: 10
    p1: 9
    p2: 0
  coverage_gaps: []
  risk_coverage:
    - risk: "subquery performance"
      covered_by: ["8.1-INT-008", "8.1-INT-009", "8.1-INT-010"]
    - risk: "backward compatibility"
      covered_by: ["8.1-UNIT-001", "8.1-INT-007"]
```

## Quality Checklist

- [x] Todos os ACs têm cobertura (AC1–AC7)
- [x] Níveis de teste apropriados (sem E2E desnecessário)
- [x] Sem cobertura duplicada entre níveis
- [x] Prioridades alinhadas com risco de negócio
- [x] IDs seguem convenção `{epic}.{story}-{LEVEL}-{SEQ}`
- [x] Cenários atômicos e independentes
