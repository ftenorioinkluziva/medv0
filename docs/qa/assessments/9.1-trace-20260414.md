# Requirements Traceability Matrix

## Story: 9.1 — Multi-Provider Support

**Date:** 2026-04-14
**Analyst:** Quinn (Test Architect)
**Linear:** BLA-72

## Coverage Summary

- Total Requirements: 6
- Fully Covered: 4 (AC1, AC2, AC5, AC6)
- Partially Covered: 1 (AC3)
- Not Covered: 1 (AC4 — E2E, fora do gate vitest)
- P0 gaps: 0
- Coverage dentro do gate vitest: ~83%

## Test Files Mapeados

- `tests/unit/resolve-model.test.ts` — 7 testes
- `tests/unit/analyze-with-agent.test.ts` — 3 testes
- `tests/unit/complete-analysis.orchestrator.test.ts` — 1 teste (síntese, cobertura parcial AC3)

---

## Requirement Mappings

### AC1 — Factory function `resolveModel` — FULL

- `resolve-model.test.ts:27` — `resolves google provider` → 9.1-UNIT-001 FULL
  - Given: modelString = 'google/gemini-2.5-flash'
  - When: resolveModel(modelString)
  - Then: google('gemini-2.5-flash') chamado, retorna {provider:'google', slug:'gemini-2.5-flash'}

- `resolve-model.test.ts:39` — `resolves openai provider` → 9.1-UNIT-002 FULL
  - Given: modelString = 'openai/gpt-4o'
  - When: resolveModel(modelString)
  - Then: openai('gpt-4o') chamado

- `resolve-model.test.ts:51` — `resolves anthropic provider when API key is set` → 9.1-UNIT-003 FULL
  - Given: ANTHROPIC_API_KEY = 'test-key', modelString = 'anthropic/claude-sonnet-4-5'
  - When: resolveModel(modelString)
  - Then: anthropic('claude-sonnet-4-5') chamado

- `resolve-model.test.ts:64` — `falls back to google when format has no slash` → 9.1-UNIT-004 FULL
  - Given: modelString = 'invalid-format'
  - When: resolveModel(modelString)
  - Then: google('gemini-2.5-flash') + console.warn com 'Invalid model format'

- `resolve-model.test.ts:80` — `falls back to google when provider is unknown` → 9.1-UNIT-005 FULL
  - Given: modelString = 'mistral/mistral-7b'
  - When: resolveModel(modelString)
  - Then: google('gemini-2.5-flash') + console.warn com 'Unknown provider'

### AC2 — `analyzeWithAgent` usa factory — FULL

- `analyze-with-agent.test.ts:50` — `passes agent.model to resolveModel` → 9.1-INT-001 FULL
  - Given: agent.model = 'google/gemini-2.5-flash'
  - When: analyzeWithAgent(agent, prompt, ctx)
  - Then: resolveModel chamado com 'google/gemini-2.5-flash'

- `analyze-with-agent.test.ts:61` — `passes resolved model to generateText` → 9.1-INT-002 FULL
  - Given: agent.model = 'openai/gpt-4o', resolveModel retorna 'resolved:openai/gpt-4o'
  - When: analyzeWithAgent(agent, ...)
  - Then: generateText chamado com {model: 'resolved:openai/gpt-4o'}

- `analyze-with-agent.test.ts:75` — `preserves behavior for existing google agents` → 9.1-INT-003 FULL
  - Given: agent.model = 'google/gemini-2.5-flash'
  - When: analyzeWithAgent(agent, ...)
  - Then: result.status === 'completed', content e tokensUsed corretos

### AC3 — Orchestrators usam factory — PARTIAL

- `complete-analysis.orchestrator.test.ts:116` — `executes foundation/specialized phases` → 9.1-INT-004 PARTIAL
  - Given: @ai-sdk/google mockado como () => 'mock-model'; generateText mockado
  - When: runCompleteAnalysis('user-1', 'doc-1', 'complete-1')
  - Then: generateText chamado (síntese executada) — resolveModel NÃO assertado explicitamente
  - Note: pipeline.ts:189 usa resolveModel, exercitado via mock do google mas sem assert direto

Gaps:
- 9.1-INT-004: resolveModel não assertado no orchestrator
- 9.1-INT-005: SYNTHESIS_MODEL env var default não testado

### AC4 — Admin form — NONE (fora do gate vitest)

Nenhum teste em tests/unit/ ou tests/e2e/ para agent-form.tsx.
Esperado — quality_gate_tools: [lint, typecheck, vitest].
Registrar como tech debt: tests/e2e/agents.spec.ts.

### AC5 — Env vars por provider — FULL

- `resolve-model.test.ts:98` — `throws when anthropic provider used without ANTHROPIC_API_KEY` → 9.1-UNIT-006 FULL
  - Given: ANTHROPIC_API_KEY deletada no beforeEach
  - When: resolveModel('anthropic/claude-sonnet-4-5')
  - Then: lança 'ANTHROPIC_API_KEY not configured'

- `resolve-model.test.ts:106` — `does not throw for openai without OPENAI_API_KEY` → 9.1-UNIT-007 FULL
  - Given: OPENAI_API_KEY deletada
  - When: resolveModel('openai/gpt-4o')
  - Then: não lança — OpenAI SDK trata em runtime

### AC6 — Testes existentes — FULL (meta-critério)

- 246/246 testes passando (Change Log, 2026-04-14, @dev)
- multi-agent-analysis.test.ts:224 cobre regressão do prefixo google/

---

## Critical Gaps

| Severidade | ID | AC | Impacto |
|-----------|-----|-----|---------|
| MEDIUM | 9.1-INT-004 | AC3 | resolveModel não assertado na síntese — CONCERNS |
| MEDIUM | 9.1-INT-005 | AC3 | SYNTHESIS_MODEL env default — CONCERNS |
| LOW | 9.1-E2E-001..003 | AC4 | Admin form UI — fora do gate |

## Gate YAML Block

```yaml
trace:
  totals:
    requirements: 6
    full: 4
    partial: 1
    none: 1
  planning_ref: 'docs/qa/assessments/9.1-test-design-20260414.md'
  uncovered:
    - ac: 'AC3-synthesis'
      reason: 'resolveModel não assertado no orchestrator — exercitado incidentalmente'
    - ac: 'AC3-env-fallback'
      reason: 'SYNTHESIS_MODEL env var default não testado'
    - ac: 'AC4'
      reason: 'Admin form UI — fora do escopo vitest desta story'
  notes: 'docs/qa/assessments/9.1-trace-20260414.md'
  p0_gaps: 0
  verdict_impact: CONCERNS
```
