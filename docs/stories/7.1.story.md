# Story 7.1 — DB Indexes & Performance

**Status:** Done
**Linear:** [BLA-64](https://linear.app/blackboxinovacao/issue/BLA-64)
**Branch:** `ftenorioemail/bla-64-story-71-db-indexes-performance`
**Cycle:** 5 (07/04–14/04/2026)
**Epic:** E7 — Production Hardening
**executor:** @dev
**quality_gate:** @qa
**quality_gate_tools:** [lint, typecheck, vitest]

---

## Story

**Como** sistema,
**Quero** índices nas foreign keys mais consultadas,
**Para** que queries de listagem e lookup tenham performance previsível com o crescimento de dados.

---

## Contexto

Atualmente só existem 3 índices custom: `snapshots(userId, createdAt)`, HNSW em `knowledge_embeddings.embedding` e GIN em `knowledge_embeddings.content_tsv`. Nenhum index existe em FKs de alta leitura como `documents.userId`, `analyses.userId`, `analyses.livingAnalysisVersionId`, `livingAnalysisVersions.livingAnalysisId` e `knowledgeBase.source`. Com dados reais, essas queries farão seq scan.

---

## Acceptance Criteria

### AC1 — Índice em `documents.userId`

**Given** a tabela `documents` sem index em `user_id`
**When** a migration é aplicada
**Then** existe index `documents_user_id_idx` em `documents(user_id)`
**And** `EXPLAIN` de `SELECT * FROM documents WHERE user_id = $1 ORDER BY created_at DESC` mostra Index Scan (não Seq Scan)

### AC2 — Índice em `analyses.userId`

**Given** a tabela `analyses` sem index em `user_id`
**When** a migration é aplicada
**Then** existe index `analyses_user_id_idx` em `analyses(user_id)`

### AC3 — Índice em `analyses.livingAnalysisVersionId`

**Given** a tabela `analyses` sem index em `living_analysis_version_id`
**When** a migration é aplicada
**Then** existe index `analyses_living_analysis_version_id_idx` em `analyses(living_analysis_version_id)`

### AC4 — Índice em `livingAnalysisVersions.livingAnalysisId`

**Given** a tabela `living_analysis_versions` sem index em `living_analysis_id`
**When** a migration é aplicada
**Then** existe index `living_analysis_versions_living_analysis_id_idx` em `living_analysis_versions(living_analysis_id)`

### AC5 — Índice em `knowledgeBase.source`

**Given** a tabela `knowledge_base` sem index em `source`
**When** a migration é aplicada
**Then** existe index `knowledge_base_source_idx` em `knowledge_base(source)`
**And** query de duplicata no auto-upload (`WHERE source = $1`) usa index

### AC6 — Migration sem downtime

**Given** banco com dados existentes (22 stories implementadas, docs/análises reais)
**When** `pnpm db:generate` e `pnpm db:migrate` são executados
**Then** migration gera apenas `CREATE INDEX` statements (não recria tabelas)
**And** dados existentes são preservados integralmente

### AC7 — Schema Drizzle atualizado

**Given** as tabelas `documents`, `analyses`, `livingAnalysisVersions`, `knowledgeBase`
**When** o schema é inspecionado
**Then** cada tabela tem callback `(table) => [index(...)]` com os novos índices
**And** `pnpm db:generate` não gera migration adicional (schema e DB em sync)

---

## Tasks

- [x] Task 1: Adicionar índices no Drizzle schema — `documents.ts` (userId), `analyses.ts` (userId, livingAnalysisVersionId, livingAnalysisId), `knowledge.ts` (source) (AC1-AC5, AC7)
- [x] Task 2: `pnpm db:generate` — gerar migration (AC6)
- [x] Task 3: `pnpm db:migrate` — aplicar migration (AC6)
- [x] Task 4: Verificar query plans com EXPLAIN (AC1, AC5)
- [x] Task 5: Lint + typecheck

---

## Dev Notes

### Padrão de índice existente no projeto

```typescript
// src/lib/db/schema/documents.ts — padrão existente
export const snapshots = pgTable('snapshots', { ... },
  (table) => [index('snapshots_user_id_created_at_idx').on(table.userId, table.createdAt)],
)
```

### Índices a criar

```typescript
// documents.ts — adicionar callback à tabela documents
(table) => [index('documents_user_id_idx').on(table.userId)]

// analyses.ts — adicionar callback à tabela analyses
(table) => [
  index('analyses_user_id_idx').on(table.userId),
  index('analyses_living_analysis_version_id_idx').on(table.livingAnalysisVersionId),
]

// analyses.ts — adicionar callback à tabela livingAnalysisVersions
(table) => [
  index('living_analysis_versions_living_analysis_id_idx').on(table.livingAnalysisId),
]

// knowledge.ts — adicionar ao knowledgeBase
(table) => [index('knowledge_base_source_idx').on(table.source)]
```

### Imports necessários

```typescript
import { index } from 'drizzle-orm/pg-core'
```

Já importado em `documents.ts` e `knowledge.ts`. Adicionar em `analyses.ts`.

---

## Scope

**IN:** Criação de 5 índices em FKs de alta leitura, migration, verificação via EXPLAIN
**OUT:** Otimização de queries, CREATE INDEX CONCURRENTLY, índices compostos

## Complexity

**S** (Small) — Alteração puramente de schema, sem lógica de negócio. ~1h estimado.

---

## Risks

- **LOW:** Migration com `CREATE INDEX` em tabela com poucos registros é instantânea. Em produção com volume alto, considerar `CREATE INDEX CONCURRENTLY` (Drizzle não gera CONCURRENTLY por padrão — aceitável para o volume atual).

---

## File List

**Modificados:**
- `src/lib/db/schema/documents.ts` — índice `documents_user_id_idx` em `documents.userId`
- `src/lib/db/schema/analyses.ts` — índices `analyses_user_id_idx`, `analyses_living_analysis_version_id_idx`, `living_analysis_versions_living_analysis_id_idx`; import `index` adicionado
- `src/lib/db/schema/knowledge.ts` — índice `knowledge_base_source_idx` em `knowledgeBase.source`

**Criados:**
- `src/lib/db/migrations/0011_slim_mockingbird.sql` — migration com 5 `CREATE INDEX` statements

---

## QA Results

**Verdict:** ✅ PASS
**Reviewer:** @qa (Quinn)
**Date:** 2026-04-10

| AC | Status |
|---|---|
| AC1 `documents_user_id_idx` | ✅ |
| AC2 `analyses_user_id_idx` | ✅ |
| AC3 `analyses_living_analysis_version_id_idx` | ✅ |
| AC4 `living_analysis_versions_living_analysis_id_idx` | ✅ |
| AC5 `knowledge_base_source_idx` | ✅ |
| AC6 Migration apenas `CREATE INDEX` | ✅ |
| AC7 Schema em sync (`db:generate` sem diff) | ✅ |

**Gates:** Lint ✅ | Typecheck ✅ | Tests 209/209 ✅

**Observations:**
- EXPLAIN Seq Scan com tabela vazia — comportamento esperado do planner. Índices ativam com volume de dados real.
- `living_analysis_versions` ausente no banco (migration 0008 registrada mas não aplicada) — incidente documentado, resolvido diretamente. Recomendação: auditoria de integridade de migrations em ambiente de staging.
- `knowledge_base_source_idx` em coluna nullable — correto para PostgreSQL btree, queries com valor não-null usarão o índice.

---

## Change Log

- 2026-04-08: Story criada pelo @po (Pax) — refinamento de AC com Given/When/Then e Dev Notes baseados no codebase real. Status: Draft.
- 2026-04-08: Validação @po (Pax) — Score 12/14 (8.6/10), GO. Adicionados Scope e Complexity. Status: Draft → Ready.
- 2026-04-10: @dev implementou. 5 índices criados, migration gerada e aplicada, lint ✅ typecheck ✅ 209/209 ✅. Status: Ready → Ready for Review.
- 2026-04-10: Nota: `living_analysis_versions` não estava no banco (migration 0008 marcada como aplicada mas tabela ausente). Tabela criada diretamente com `CREATE TABLE IF NOT EXISTS` + FKs + índices via script.
- 2026-04-16: PR #24 merged (commit 698c667, 2026-04-10). Story closed. Status: In Review → Done. — Pax (@po)
