# Story 1.2 — Authentication + Reset de Senha

**Status:** Done
**Linear:** BLA-37
**Branch:** `ftenorioemail/bla-37-story-12-authentication-reset-de-senha`
**Cycle:** 1 (30/03–06/04/2026)

---

## Story

**Como** paciente,
**Quero** me registrar, fazer login e recuperar minha senha por email,
**Para** que minha conta seja segura e acessível mesmo se esquecer a senha.

---

## Contexto

NextAuth v5 (Auth.js) com Credentials provider. Reset de senha via Resend (email transacional). Tokens de reset armazenados na tabela `password_reset_tokens` com TTL de 1 hora. Sem OAuth providers no MVP.

---

## Acceptance Criteria

### AC1 — NextAuth v5 configurado
* `next-auth@beta`, `@auth/drizzle-adapter` instalados
* `lib/auth/config.ts` com Credentials provider
* `bcryptjs` para hash de senha no registro e verificação no login
* `app/api/auth/[...nextauth]/route.ts` configurado
* `middleware.ts` protege todas as rotas em `app/(app)/`

### AC2 — Schema `users` completo
* Campos: id (UUID), email (unique), passwordHash, role (enum: patient/admin default patient), onboardingCompleted (bool default false), isActive (bool default true), createdAt, updatedAt
* Tabela `password_reset_tokens`: id, userId (FK), token (UUID único), expiresAt (now + 1h), usedAt (nullable), createdAt

### AC3 — Fluxo de registro
* `POST /api/auth/register` ou Server Action em `app/(auth)/register/`
* Validação Zod: email válido, senha mínimo 8 caracteres
* Erro claro se email já cadastrado
* Redireciona para onboarding após registro bem-sucedido

### AC4 — Fluxo de login
* Tela de login com campos email/senha
* Erro genérico em credenciais inválidas (não expõe se email existe)
* Sessão JWT com `user.id`, `user.email`, `user.role`, `user.onboardingCompleted`
* Redirect pós-login: onboarding se `onboardingCompleted = false`, dashboard se true

### AC5 — Reset de senha
* Tela "Esqueci minha senha" → `POST` com email → gera token → envia email via Resend
* Email com link: `{baseUrl}/auth/reset-password?token={uuid}`
* Tela de nova senha: valida token (não usado, não expirado) → atualiza `passwordHash` → marca token como usado
* Erro claro para token inválido ou expirado

### AC6 — Email via Resend
* `resend` package instalado
* `RESEND_API_KEY` e `RESEND_FROM_EMAIL` em `.env`
* Template do email em `lib/email/templates/reset-password.tsx` (React Email)
* Envio assíncrono — não bloqueia resposta ao usuário

### AC7 — Telas mobile-first
* `/auth/login`, `/auth/register`, `/auth/forgot-password`, `/auth/reset-password`
* Shadcn UI: Card, Form, Input, Button
* Viewport 390px como referência de design

---

## Definition of Done

- [x] Todos os ACs atendidos
- [x] Vitest: registro duplicado retorna erro, login inválido retorna erro, token expirado retorna erro
- [x] Playwright (MCP): fluxo completo registro → login → logout em viewport 390px
- [x] `pnpm lint` e `pnpm typecheck` sem erros
- [x] Context7 consultado para NextAuth v5 (Auth.js) e Resend SDK APIs atuais

---

## Tasks

- [x] Task 1: Instalar `next-auth@beta`, `@auth/drizzle-adapter`, `bcryptjs`, `resend`, `react-email`
- [x] Task 2: Configurar `lib/auth/config.ts` com Credentials provider + JWT strategy
- [x] Task 3: Criar `app/api/auth/[...nextauth]/route.ts` e `middleware.ts`
- [x] Task 4: Criar Server Action de registro com validação Zod
- [x] Task 5: Criar telas `/auth/login`, `/auth/register`, `/auth/forgot-password`, `/auth/reset-password`
- [x] Task 6: Implementar fluxo de reset de senha (gerar token → enviar email → validar token → nova senha)
- [x] Task 7: Criar template de email `lib/email/templates/reset-password.tsx`
- [x] Task 8: Escrever testes Vitest para os fluxos de auth
- [x] Task 9: Validar com Playwright em viewport 390px

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log References
- `login-error.png` — captura de tela do fluxo de erro de login
- `register-validation.png` — captura de tela da validação de registro

### Completion Notes
- NextAuth v5 usa `auth()` em Server Components (não `getServerSession()`)
- `middleware.ts` na raiz do projeto (não em `src/`)
- Build errors de produção resolvidos no commit `cc5b755` (fix)
- PR criado: [feat(bla-37): Authentication + Password Reset](https://github.com/ftenorioinkluziva/medv0/pull/2)

### File List
- `src/lib/auth/config.ts` — configuração NextAuth v5 com Credentials
- `src/app/api/auth/[...nextauth]/route.ts` — handler Auth.js
- `middleware.ts` — proteção de rotas `app/(app)/`
- `src/app/(auth)/login/page.tsx` — tela de login
- `src/app/(auth)/register/page.tsx` — tela de registro
- `src/app/(auth)/forgot-password/page.tsx` — tela de esqueci minha senha
- `src/app/(auth)/reset-password/page.tsx` — tela de nova senha
- `src/lib/email/templates/reset-password.tsx` — template React Email
- `src/lib/actions/auth.ts` — Server Actions de registro e reset

### Change Log
- 2026-03-23: Story criada a partir de BLA-37
- 2026-03-24: Implementação completa — feat(bla-37) commitado
- 2026-03-24: Build errors corrigidos — fix(bla-37) commitado
- 2026-03-24: Status marcado como Done no Linear; adicionado ao Cycle 1
